'use strict';

var Log = require('./node');

var loggers = {};

function log(namespace) {
  var log = loggers[namespace];
  if (!log) loggers[namespace] = log = new Log(namespace);
  return log;
}

module.exports = log;