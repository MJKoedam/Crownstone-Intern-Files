import { CloudRequestorInterface } from "../tools/requestors";
import { Crownstones } from "./crownstones";
import { Locations } from "./locations";
export declare class Spheres {
    rest: CloudRequestorInterface;
    filter: any;
    sphereIds: any[];
    constructor(cloudRequestor: CloudRequestorInterface, filter?: filter);
    downloadAllSpheres(): Promise<cloud_Sphere[]>;
    resolveIdentifier(): Promise<void>;
    crownstones(filter: filter): Crownstones;
    locations(filter: filter): Locations;
    users(): void;
    keys(): Promise<cloud_Keys[]>;
    data(): Promise<cloud_Sphere[]>;
    refresh(): Promise<this>;
}
