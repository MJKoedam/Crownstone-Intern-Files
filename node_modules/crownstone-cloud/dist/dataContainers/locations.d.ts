import { CloudRequestorInterface } from "../tools/requestors";
import { Crownstones } from "./crownstones";
import { Spheres } from "./spheres";
export declare class Locations {
    locationIds: string[];
    spheres: Spheres;
    filter: filter;
    rest: CloudRequestorInterface;
    constructor(cloudRequestor: CloudRequestorInterface, spheres?: Spheres, locationFilter?: filter);
    refresh(): Promise<this>;
    data(): Promise<cloud_Location[]>;
    _getFilteredData(): any[];
    _searchInCache(): boolean;
    resolveIdentifier(): Promise<void>;
    downloadAllLocations(): Promise<cloud_Location[]>;
    crownstones(filter: filter): Crownstones;
}
